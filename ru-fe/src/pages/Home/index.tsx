import { FunctionComponent } from 'preact';
import './style.css';

export const Home: FunctionComponent = () => {
  return (
    <div class="home">
      <main>
        <section class="hero">
        </section>
      </main>
    </div>
  );
}